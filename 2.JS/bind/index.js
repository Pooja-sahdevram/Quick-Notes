let pi = {
  cahi: "tea",
  price: 55,
  isAvailable: true,
};

// Using for...in loop
for (let key in pi) {
  if (pi.hasOwnProperty(key)) {
    console.log(key + ": " + pi[key]);
  }
}
let x = 5;

let * pil;
console.log(pil);
